let currentTab = "all"
const tabActive = ["bg-nevy", "border-navy"]
const tabInactive = ["bg-transparent", "text-slate-700", "border-state-200"]
function switchTab(tab) {
    console.log(tab)
    const tabs = ["all", "interview", "rejected"];

    for (const i of tabs) {
        const tabName = document.getElementById("tab-" + i );

        
    }
}